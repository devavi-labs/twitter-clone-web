import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles(() => ({
  optionsButton: {
    opacity: 0.8,
    width: 36,
    height: 36,
    padding: 8,
    margin: 0,
  },
}));
