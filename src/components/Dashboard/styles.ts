import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles(() => ({
  dashboard: {
    flex: 1,
    display: "flex",
    position: "relative",
    width: "100%",
    maxWidth: 1440,
    margin: "0 auto",
  },
}));
